# feature-git-tester

A tiny random-feature Python program that demonstrates multiple annotated feature blocks.

## Run the program

```bash
python main.py
```

Each run selects a few random features and prints a small result.

## Feature annotations

The code uses markers like `# &[FeatureA]` and `# &[FeatureA]` around each feature implementation.
